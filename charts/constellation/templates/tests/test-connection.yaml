apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "constellation.fullname" . }}-test-connection"
  labels:
    {{- include "constellation.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "constellation.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
